int i, *ip, f(), ff(a, b), *fip(), (*pfi) (), *(*pfpi) ();
int b 0;
int (*c) ();
register d 1;
auto **e;
float fa[1+7], *afp[17];
static int x3d[3][5][7];
int arr[] {1, 2, 3};
struct	tname	funcblk { NAME, 0, 0, REG, 0, 0 };
struct {
	int	op;
	int	type;
	char	ssp;		/* subscript list */
	char	lenp;		/* structure length */
};
struct a {
    int count, cnt[20];
};
struct a xyz, *abc;
struct tnode {
    char tword[20];
    int count;
    struct tnode *left;
    struct tnode *right;
};
struct kwtab {
	char	*kwname;
	int	kwval;
} kwtab[]
{
	"int",		INT,
};

int a 1;
int *b { a };
char *c "INT";
int d 'a';
char e 0177;
int f &y[1];


